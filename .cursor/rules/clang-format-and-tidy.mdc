---
description: 每次写完或改完 C/C++ 后必须立刻跑 clang-format 与 clang-tidy
alwaysApply: true
---

# clang-format + clang-tidy（强制）

每次创建或修改任何 C/C++ 源文件后，**必须立刻**对本轮改过的文件依次执行 **clang-format** 与 **clang-tidy**，两者都通过后才能结束该轮改动。禁止只改内容不检查；禁止只跑其中一个。

## 范围

- 包含：`Source/` 下的 `.h` / `.hpp` / `.c` / `.cc` / `.cpp` / `.cxx` / `.inl`
- 排除：`ThirdParty/`、生成目录 `Build/`、以及未改动的文件

## 1. clang-format（格式）

在仓库根目录（含 `.clang-format` 的那一层）对每个改过的文件执行：

```
clang-format -i --style=file <相对路径>
```

多文件一次跑完；不要手改缩进/换行去“对齐风格”，一律交给 clang-format。

## 2. clang-tidy（命名与静态检查）

在仓库根目录（含 `.clang-tidy` 的那一层）对每个改过的文件执行。优先使用编译数据库：

```
clang-tidy -p Build/Debug --config-file=.clang-tidy <相对路径>
```

`-p` **只许** Debug 预设产物 `Build/Debug`。若没有 `Build/Debug/compile_commands.json`，先 `cmake --preset debug` 再跑 tidy，禁止改用 Release / RelWithDebInfo。仍没有 compile_commands 时，至少用配置文件直接检查命名：

```
clang-tidy --config-file=.clang-tidy <相对路径> -- -std=c++26
```

- 有报错必须先修到干净，再结束本轮
- 命名以 `.clang-tidy` 的 `readability-identifier-naming` 与 `stardust-conventions` 为准
- CLion 惯用法检查：`CppUseAuto`、`CppUseDesignatedInitializers`（见 `.idea/inspectionProfiles/Project_Default.xml`）
- 若使用 `clang-tidy -fix`，修完后**再跑一遍** `clang-format -i`

## 注意

- 即使只改一行、新增空文件、或认为已经符合风格，也必须跑 format **和** tidy
- 不要对第三方或生成代码执行
- format 管排版；tidy 管命名等静态规则——两者都不可省略
