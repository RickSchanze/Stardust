---
description: 每次写完或改完 C/C++ 代码后必须立刻 clang-format
alwaysApply: true
---

# clang-format（强制）

每次创建或修改任何 C/C++ 源文件后，**必须立刻**用仓库根目录 `.clang-format` 对**本次改过的文件**执行 `clang-format -i`，再结束该轮改动。禁止只改内容不格式化。

## 范围

- 包含：`Source/` 下的 `.h` / `.hpp` / `.c` / `.cc` / `.cpp` / `.cxx` / `.inl`
- 排除：`ThirdParty/`、生成目录 `Build/`、以及未改动的文件

## 做法

在仓库根目录（含 `.clang-format` 的那一层）对每个改过的文件执行：

```
clang-format -i --style=file <相对路径>
```

多文件一次跑完；不要手改缩进/换行去“对齐风格”，一律交给 clang-format。

## 注意

- 即使只改一行、新增空文件、或认为已经符合风格，也必须跑
- 不要对第三方或生成代码执行格式化
- 命名规则仍由 `.clang-tidy` / `stardust-conventions` 约束，clang-format 不管命名
