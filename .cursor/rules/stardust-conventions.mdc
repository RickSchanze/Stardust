---
description: Stardust UE 风格命名与 C++/CMake 工程约定（所有对话强制遵守）
alwaysApply: true
---

# Stardust 工程约定

## 技术栈

- C++26（GCC 16+），优先使用语言/标准库新特性（含静态反射 `^^` / `std::meta`）
- 构建：CMake + Ninja；**编译验证必须用 `CMakePresets.json` 的 debug 预设**（`cmake --preset debug` / `cmake --build --preset debug`，产物在 `Build/Debug`）。Release / RelWithDebInfo 仅在用户明确要求时使用
- 目录：`Source/` 引擎代码，`ThirdParty/` 第三方（**仅 git submodule**），根与 `Source/` 各有 `CMakeLists.txt`

## 命名（UE 风格，强制）

| 类别 | 规则 | 示例 |
|------|------|------|
| 命名空间 | 大驼峰 | `Stardust::Render` |
| 类 / 结构体 / 枚举 / 类型别名 | 大驼峰 | `RenderDevice` |
| 函数 / 方法 | 大驼峰 | `CreateBuffer` |
| 局部变量 / 参数 | 大驼峰 | `BufferSize` |
| public 成员（含 static） | 大驼峰，无前缀 | `DeviceName` / `static Instance` |
| protected / private 成员（含 static） | `m` + 大驼峰 | `mDeviceName` / `static mInstance` |
| 文件作用域全局变量 / 命名空间 `inline constexpr` | `g` + 大驼峰 | `gCommandAlignment` / `gEngine` |
| 静态成员 | **无**单独前缀，按访问属性 | private → `mInstance`；public → `Instance` |

说明：clang-tidy 把静态数据成员归为 `ClassMember`（不是 `PrivateMember`）；未配置时会误判为全局变量并强行加 `g`。已在 `.clang-tidy` 用 `ClassMember` + `ClassMemberIgnoredRegexp` 放行 `m` 前缀。命名空间内 `inline constexpr` 视为 **GlobalConstant**，同样必须 `g` 前缀。


例外：

- C/C++ 链接入口 `main` / `WinMain` 保持小写
- 第三方库 API 与类型保持原样（如 `spdlog::info`、`glm::vec3`、`mi_version`）
- 引擎核心类型（`String` / `StringView` / `Logger` 等）**不**放在 `Stardust` 命名空间；模块用目录表达（`Core/`、`RHI/`）

## 宏（强制）

- **默认**：宏只用于条件编译（`#if` / `#ifdef` 等），例如 `STARDUST_LOG_WITH_FILE_INFO`
- **特例**：日志宏 `LogDebug` / `LogInfo` / `LogWarn` / `LogError` / `LogCritical` / `LogAssert`（`Core/Logging/LogMacros.h`）——用预处理拼接 `"[Category] " Format` 字面量，避免运行期拼 format
- 日志 Category 参数为 `Logcat` 枚举项名（如 `Render`），枚举本身保留作分类清单；禁止再扩大其它业务宏
- 常量 / 内联逻辑 / 类型别名仍用 `constexpr` / `using` 等，不用宏

## 格式与静态检查（强制）

- 遵循根目录 `.clang-format`（Allman 大括号、4 空格；初始化列表逗号在行尾，禁止 `, Foo` 式 Leading comma；`#if` / `#include` / `#define` 不随嵌套缩进）
- 命名由根目录 `.clang-tidy` 的 `readability-identifier-naming` 约束
- **每次写完/改完 `Source/` 下 C/C++ 文件后必须立刻跑 clang-format 与 clang-tidy**，两者都通过才能结束本轮；详见 `clang-format-and-tidy.mdc`
- 禁止只改内容不检查；禁止只跑 format 或只跑 tidy

## C++ 惯用法（与 CLion 检查对齐）

- **指定初始化（designated initializers）**：**聚合类型 / POD struct** 初始化时，多字段或嵌套成员（如 `Command`、各类 `*Command`、Desc）优先用指定初始化；带成员函数的 class（如 `CommandBuffer`）对其成员 struct 字段可用指定初始化，但 class 本身仍用默认构造 + 赋值
  - 正确：`Command Header{.Type = TypeValue};`、`DrawCommand{.Header = {.Type = DrawCommand::TypeValue}, .VertexCount = 3};`
  - 错误：`Command Header{TypeValue};`、`DrawCommand Command{}; Command.VertexCount = 3;`
- **`auto`**：局部变量、以及 `static constexpr` 成员在右侧类型可明确推断时使用 `auto`（CLion `CppUseAuto`）
  - 正确：`const auto Offset = AlignCommandOffset(...);`、`static constexpr auto TypeValue = CommandType::Draw;`
  - 例外：需要明确接口类型、窄化风险、或 `auto` 降低可读性时保留显式类型
- **值类型形参加 `const`**（CLion `CppParameterMayBeConst`）：函数/方法的值类型形参（基础数值类型、枚举、按值传递的 Handle 等）若在函数体内不被修改，必须声明为 `const`
  - 正确：`void Destroy(const BufferHandle Handle);`、`void Draw(const UInt32 VertexCount, const UInt32 InstanceCount = 1);`
  - 错误：`void Destroy(BufferHandle Handle);`、`void Draw(UInt32 VertexCount);`
  - 注意：引用 / 指针形参遵循各自 const-correctness 规则；本条仅针对**值传递**的形参
- CLion 项目检查已启用：`CppUseAuto`、`CppUseDesignatedInitializers`、`CppParameterMayBeConst`（见 `.idea/inspectionProfiles/Project_Default.xml`）
- **clang-tidy 例外**：`Source/` 外不检查；容器/分配器的 STL 接口名（`begin`/`end`/`value_type`/`allocate` 等）保持标准库命名，见 `.clang-tidy` 的 `*IgnoredRegexp`

## CMake / 第三方

- 源文件不放仓库根目录；目标在 `Source/CMakeLists.txt`
- **禁止 FetchContent**；第三方只许 `git submodule` + `ThirdParty/CMakeLists.txt` 的 `add_subdirectory`，并链到 `StardustThirdParty`
- 网络拉取失败时使用本地代理 `http://127.0.0.1:7890`
